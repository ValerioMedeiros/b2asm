MACHINE
	BIT_VECTOR

SEES 
	BIT_DEFINITION,
	BIT_VECTOR_DEFINITION

CONSTANTS
	BV8,
	BV8_INDEX,
	BV8_SIZE

PROPERTIES
	BV8_SIZE = 8 &
	BV8_INDEX = (0..(BV8_SIZE - 1)) &
	BV8 <: BIT_VECTOR8 &
	BV8 = { vv | vv : BIT_VECTOR8 & bv_size(vv) = BV8_SIZE }
END